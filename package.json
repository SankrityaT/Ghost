{
  "name": "ghost-agent",
  "version": "1.0.0",
  "description": "An iMessage agent that catches you before you ghost someone — and drafts the reply.",
  "type": "module",
  "scripts": {
    "start": "bun run src/agent.ts",
    "dev": "bun run --watch src/agent.ts"
  },
  "dependencies": {
    "@ai-sdk/groq": "^3.0.31",
    "@photon-ai/imessage-kit": "^2.1.2",
    "ai": "^6.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
